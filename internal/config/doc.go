/*
Package config handles the loading, saving, and defaults for ~/.dossier/config.yaml.

It defines the workspace root, author identity, configured launch profile,
configurable discussion interfaces and leads, and optional team-sync settings.
It exposes helper methods
to map user configuration into core.Config parameters consumed by core.Service.
*/
package config
